# Target Profile: Example Corp

*Generated: 2025-01-01 | Agent: argus (Target Profiler)*

## Quick Facts
- **Company**: Example Corp
- **Industry**: SaaS / B2B
- **Size**: 200-500 employees
- **Bug Bounty**: HackerOne (private program)
- **Bounty Range**: $500 (Low) - $15,000 (Critical)
- **Response Time**: ~48 hours (triaged), ~14 days (resolved)
- **Scope**: `*.example.com`, `api.example.io`, `admin.example.com`
- **Exclusions**: `docs.example.com`, `status.example.com`, third-party CDN

## Technology Stack
| Layer | Technology | Version Clues |
|-------|-----------|---------------|
| Frontend | React 18 + Next.js | `__NEXT_DATA__`, `/__next/` paths |
| API | GraphQL (Apollo Server) | `/graphql` endpoint, Apollo headers |
| Backend | Node.js + Express | `X-Powered-By: Express` |
| Database | PostgreSQL | Error messages reference `pg_` |
| Auth | JWT (RS256) + OAuth 2.0 | JWK endpoint at `/.well-known/jwks.json` |
| CDN | Cloudflare | `cf-ray`, `cf-cache-status` headers |
| Cloud | AWS (us-east-1) | Certificate SAN includes `*.us-east-1.elb.amazonaws.com` |
| Storage | AWS S3 | `*.s3.amazonaws.com` CNAME records |

## Attack Surface
### High Priority
1. **GraphQL `/graphql`** — Check introspection, field suggestions, batching, depth limit
2. **JWT Authentication** — Test algorithm confusion (RS256→HS256), JWK injection
3. **OAuth Flow** — Test redirect_uri validation, state parameter, CSRF
4. **Admin Panel `admin.example.com`** — Auth bypass, privilege escalation

### Medium Priority
5. **API `api.example.io`** — IDOR on user resources, rate limiting
6. **File Upload** — Avatar/profile picture upload
7. **Webhook Integration** — SSRF potential in webhook URL configuration

### Low Priority
8. **Search `example.com/search`** — XSS, SQLi in search parameter
9. **Password Reset** — Token enumeration, host header injection

## Historical Incidents
- 2023: Researcher found IDOR on `/api/users/{id}` — fixed within 72 hours
- 2022: S3 bucket `example-corp-assets` was publicly readable — immediately remediated
- No known breaches or major security incidents

## Recon Notes
- 45 subdomains discovered (passive)
- 23 live HTTP services
- 2 GraphQL endpoints (main + admin)
- 4 AWS S3 buckets discovered (1 previously public)
- JavaScript source maps exposed on staging subdomain
- Employee GitHub repos contain `.env.example` with internal service URLs

## Attack Paths (Generated by Odysseus)

### Path Alpha: JWT Confusion → Auth Bypass
- **Exploitability**: 0.7 | **Impact**: CRITICAL | **Bounty**: $10,000-$15,000
- JKU header points to `/.well-known/jwks.json` — test with attacker-controlled JWK
- If RS256→HS256 works with public key as secret: full auth bypass

### Path Beta: GraphQL Introspection → Batching Attack
- **Exploitability**: 0.85 | **Impact**: MEDIUM | **Bounty**: $1,500-$3,000
- Introspection query → full schema map
- Query batching to bypass rate limits
- Field suggestions may reveal internal admin mutations

### Path Gamma: Webhook SSRF → AWS Metadata
- **Exploitability**: 0.6 | **Impact**: CRITICAL | **Bounty**: $7,500-$12,000
- Webhook URL in integration settings — replace with AWS metadata endpoint
- DNS rebinding may be needed to bypass domain allowlist

## Program Rules
- Rate limit: 5 req/sec
- No automated scanning during business hours (9-5 EST)
- No social engineering, physical testing, DoS
- Report within 24 hours of discovery
